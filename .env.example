# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://ordersDbReader:7HFtko7GNplIIi11@orders-flex-cluster.wbz5pht.mongodb.net/

# Database name (if not in URI)
MONGODB_DATABASE=orders-db

# ─── TOGETHER API (Stage 2) ─────────────────────────────────────────
TOGETHER_API_KEY=key_CYw91ypJrCBLnvwBksqvf

# ─── SECURITY FILTERS ───────────────────────────────────────────────
# Comma-separated list of collections that cannot be queried
BLOCKED_COLLECTIONS=salaries,auth_tokens,employee_personal,sessions

# Comma-separated list of fields to strip from ALL query results
BLOCKED_FIELDS=password,password_hash,aadhaar,pan_number,phone,mobile,email,customer_name,bank_account,ifsc_code,auth_token,refresh_token,secret

# Regex patterns for additional field matching (catches variations like phone_number, email_address)
REDACT_PATTERNS=password,aadhaar,pan,phone,mobile,email,bank,ifsc,token,secret

# Maximum number of documents returned per query
MAX_RESULTS=100
